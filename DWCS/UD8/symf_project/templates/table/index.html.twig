{% extends 'base.html.twig' %}

{% block title %}Hello TableController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .title{color: red;}
</style>

<div class="example-wrapper">
    <h1 class ="title">Hello {{ controller_name }}! ✅</h1>

    <p>Filas: {{fils}}</p>
    <p>Columnas: {{cols}}</p>

    <table class = "table">
    <thead>
        <tr>
            {% for i in 1..cols %}
                <th>Col.{{i}}</th>
            {% endfor %}
        </tr>
    </thead>
    <tbody>
     {% for row in table_array %}
        <tr>
            {% for cell in row %}
                <td><a href= "{{path('app_lucky_number',{'max': cell})}}">{{ cell }}</a></td>
            {% endfor %}
        </tr>
    {% endfor %}
    </tbody>
    </table>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>C:/Users/a23braisbm/Desktop/DUAL/DWCS/UD8/symf_project/src/Controller/TableController.php</code></li>
        <li>Your template at <code>C:/Users/a23braisbm/Desktop/DUAL/DWCS/UD8/symf_project/templates/table/index.html.twig</code></li>
    </ul>
</div>
{% endblock %}
